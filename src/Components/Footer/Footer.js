import React from "react";
import { Box } from "@mui/system";

const Footer = (props) => {
  return (
    <>
      <Box sx={{ p: 6, background: "blue" }}>Hey this is the footer</Box>
    </>
  );
};

export default Footer;
